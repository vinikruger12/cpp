#include <bits/stdc++.h>

using namespace std;

int main(){
    int n; cin >> n;
    int r; cin >> r;
    int l;
    set<int> seta;
    for(int i = 1;i <= r;i++){
        seta.insert(i);
    }

    for(int i = 0;i < r;i++){
        cin >> l;
        
    }
}