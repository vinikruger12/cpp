#include <bits/stdc++.h>

using namespace std;
signed main(){
    ios_base::sync_with_stdio(0);cin.tie(0);
    int x, y; cin >> x >> y;
    int a = ((x+y)/2);
    int b = x - a;

    cout << a << " " <<  b << '\n'; 
}