#include <bits/stdc++.h>
using namespace std;

int main() {
    int t; cin >> t;
    while (t--) {
               int n,m,l,r;
               cin >> n >> m >> l >> r;
               vector<int> vec(r-l);
               for(int i = l;i < r;i++){
                vec[i] = i;
               }

               int menor 
} 

}
