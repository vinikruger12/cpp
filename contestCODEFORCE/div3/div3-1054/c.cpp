#include <bits/stdc++.h>
    
using namespace std;

vector<int> a;
int n;
int res = 0;

void mex(){
    int menor;
    for(int i = 0;i < n;i++){
        
    }
}


int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t; cin >> t;
    while(t--){
        int n, k; cin >> n >> k;
        a.resize(n);
        for(int i = 0;i < n;i++){
            cin >> a[i];
        }
        
    }
}